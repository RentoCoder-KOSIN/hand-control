[project]
name = "hand-control"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "RentoCoder-KOSIN", email = "miuranchi_6950@outlook.com" }
]
requires-python = ">=3.12"
dependencies = [
    "mediapipe>=1.0.1",
    "numpy>=2.5.2",
    "opencv-python>=5.0.0.93",
    "pyautogui>=0.9.54",
]

[project.scripts]
hand-control = "hand_control.hand_screen_control:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
